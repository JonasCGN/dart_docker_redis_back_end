# Iniciar a imagem do docker api

Linux
sudo docker build -t my_api:latest .

Windows
docker build -t my_api:latest .

# Iniciar um swarm localmente

docker swarm init
docker stack deploy --compose-file docker-stack.yml SO

# verificar serviço

docker service ls

# verificar os node

docker node ls